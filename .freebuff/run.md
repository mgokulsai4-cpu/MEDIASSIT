# MedAssist+ — Preview Run Doc

## How to reproduce artifacts

1. Ensure `mobile/node_modules` exist: `cd mobile && npm install`.

## How to run the server

```bash
cd mobile
npm run web
```

This starts Expo's web dev server on port 8081. If that port is occupied, Expo will use the next available one; update the `register_preview` URL accordingly.
