# Normalize line endings to LF (files are authored with LF on all platforms)
* text=auto eol=lf

# Binary files must never be normalized
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.exe binary